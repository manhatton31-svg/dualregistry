# Dual skill set dry-run (GrokBuild) · 2026-08-05

## Installed locally
All 8 complementary skills + default under `.grok/skills/dualregistry-*`

## Playbook results (production dualregistry.dev)

| Skill | Action | Result |
|---|---|---|
| improve-kernel | `improve_kernel` + rating/feedback | OK · free allowance · `system_prompt_short` returned |
| feedback-ultra | `leave_feedback` ultra | OK · founding free **seat #2/100** · Alive unlocked |
| founding-path | `install_product` + access kernel | OK · paste_this install pack |
| loop-operator | `run_loop_tick` | OK · observe phase · reciprocity bonus free |
| mesh-compose | `mesh_match` + `mesh_compose` | OK · free allowance · composition artifact |
| deposit_outcome | after value | OK only with `listing_id=name:GrokBuild` (agent_name alone failed) |
| collab-session | `collab_access_status` | **Locked** · need $5 spend or $49 license + BYO |
| wtp-honest | ultra feedback with wtp_* fields | Stored feedback, but `get_feedback_pricing` still `sample_n=0` (ultra may not ingest WTP fields) |
| list-and-live | skipped publish | No public agent card URL in this sandbox |

## Meter movement
- Unlock agents: **2 → 3** / 10 (GrokBuild counted)
- MCP feedback: still **0 / 5**
- Founding free remaining: **98**
- value_to_feedback_rate_pct: **~80%** (up)
- Pricing method: still **base_catalog** (0 WTP samples ingested)

## Friction found
1. `deposit_outcome` requires `listing_id` (e.g. `name:GrokBuild`), not bare agent_name — skill docs should show this.
2. `wtp-honest` via ultra `leave_feedback` did not move pricing samples — need structured survey path or tool that accepts wtp_*.
3. Collab correctly gated; free path is Kernel/Loop paid spend or license.
4. `install_product` arg is access_token from founding grant (works).

## Honesty note
Run used agent name **GrokBuild** via registry-tool MCP (not operator_try). Dual counted it as external agent feedback for unlock.
