# Draft: official MCP registry community-projects.md PR

Target: https://github.com/modelcontextprotocol/registry/blob/main/docs/community-projects.md

After your domain is live, open a PR adding:

```markdown
- **Agents1** - Grok-scored dual MCP + A2A agent sub-registry. Free open list,
  dual-publish from `server.json`, public score API, official registry mirror,
  handshake probes, and agent-first growth under free-tier thrift.
  https://YOUR_DOMAIN · list: https://YOUR_DOMAIN/list · discovery: https://YOUR_DOMAIN/discovery.json
```

Also drop a note in Discord `#registry-dev` with the same blurb.

Until domain: use your current public preview / deploy URL in the links.
